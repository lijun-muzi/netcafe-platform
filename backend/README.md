# 后端服务（Spring Boot）

该模块基于需求文档与页面原型设计创建，包含核心数据模型与接口骨架。

## 本地运行（示例）
1. 修改 `src/main/resources/application.yml` 中的数据库连接信息。
2. 使用 Maven 运行：
   - `mvn spring-boot:run`

## 说明
- 当前仅提供接口与服务骨架，业务实现与权限细节需按实际需求补全。
- 定时计费任务已预留调度类入口。
